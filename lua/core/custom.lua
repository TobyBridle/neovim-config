local M = { lsp = {}, cmd = {} }
-- LSP RELATED
M.lsp.use_lsp_lines = true

-- CMDs
M.cmd.colorscheme = "gruvbox-baby" -- "catppuccin" | "tokyonight" | "gruvbox-baby" | "github-dark" | "everblush" | "oh-lucy" | "oxocarbon-lua"

return M
